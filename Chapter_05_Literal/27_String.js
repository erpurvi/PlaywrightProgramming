// Single quotes
let single = 'Hello World';
let withDouble = 'She said "hi"';
console.log( single );
console.log( withDouble );

// Double quotes
let double = "Hello World";
let withSingle = "It's a test";
console.log( double );
console.log( withSingle );

//javascript does not have a character type. It only has a string type. A string can be a single character or multiple characters. Javascript loves single character mostly because it is a single quote. But it can also be a double quote. It does not matter. It is still a string.
let c = 'c';
let c1 = 'cc';
console.log( typeof c );
console.log( typeof c1 );
console.log( typeof double );

// 'JavaScript prefers to use single code. '